<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
/>
<!--        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"-->
<!--        app:spanCount="2" />-->
    <LinearLayout
        android:id="@+id/liner_container_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center"
        android:layout_margin="20dp"
        android:background="@color/black"
        >
      <Button
        android:id="@+id/btn_grid_layout"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="Get GridLayout"
        android:layout_margin="10dp"/>

        <Button
            android:id="@+id/btn_linear_layout_vertical"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="Get LinearLayout Vertical"
            android:layout_margin="10dp"/>

        <Button
            android:id="@+id/btn_linear_layout_horizontal"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:layout_margin="10dp"
            android:text="Get LinearLayout Horizontal" />
</LinearLayout>

</FrameLayout>